import AdminPagePlaceholder from "@/components/common/AdminPagePlaceholder";

export default function CottageGalleryPage() {
  return (
    <AdminPagePlaceholder
      title="Cottage Gallery"
      description="Manage cottage image uploads using the backend single-image upload endpoint."
    />
  );
}
