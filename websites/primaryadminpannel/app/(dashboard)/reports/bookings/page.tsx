import AdminPagePlaceholder from "@/components/common/AdminPagePlaceholder";

export default function BookingReportsPage() {
  return (
    <AdminPagePlaceholder
      title="Booking Reports"
      description="Review booking performance when backend report endpoints are available."
    />
  );
}
