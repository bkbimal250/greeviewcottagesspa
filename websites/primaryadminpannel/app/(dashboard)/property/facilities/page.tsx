import AdminPagePlaceholder from "@/components/common/AdminPagePlaceholder";

export default function PropertyFacilitiesPage() {
  return (
    <AdminPagePlaceholder
      title="Property Facilities"
      description="Manage facility values stored on the backend property record."
    />
  );
}
