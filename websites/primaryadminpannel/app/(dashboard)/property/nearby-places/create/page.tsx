import AdminPagePlaceholder from "@/components/common/AdminPagePlaceholder";

export default function CreateNearbyPlacePage() {
  return (
    <AdminPagePlaceholder
      title="Create Nearby Place"
      description="Add a nearby place with name, category, distance, travel time and map URL."
    />
  );
}
