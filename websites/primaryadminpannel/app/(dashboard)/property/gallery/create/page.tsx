import AdminPagePlaceholder from "@/components/common/AdminPagePlaceholder";

export default function UploadPropertyImagePage() {
  return (
    <AdminPagePlaceholder
      title="Upload Property Image"
      description="Upload a property image with image_type and image fields."
    />
  );
}
