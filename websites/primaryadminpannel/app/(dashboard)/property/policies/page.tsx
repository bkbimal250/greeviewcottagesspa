import AdminPagePlaceholder from "@/components/common/AdminPagePlaceholder";

export default function PropertyPoliciesPage() {
  return (
    <AdminPagePlaceholder
      title="Property Policies"
      description="Manage house rules, cancellation, refund, child, pet and guest policies."
    />
  );
}
