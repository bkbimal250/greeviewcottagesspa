import AdminPagePlaceholder from "@/components/common/AdminPagePlaceholder";

export default function CreatePropertyPolicyPage() {
  return (
    <AdminPagePlaceholder
      title="Create Policy"
      description="Add a policy value to the backend property record."
    />
  );
}
